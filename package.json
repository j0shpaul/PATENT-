{
  "name": "patent-plus",
  "version": "1.0.0",
  "description": "PATENT+ — Enterprise Legal-Tech Portfolio Intelligence & Renewal Decision Platform",
  "scripts": {
    "dev:backend": "uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload",
    "dev:frontend": "cd frontend && npm run dev",
    "seed": "python3 -c 'from backend.db import init_db; from backend.providers.seed_generator import seed_database; init_db(); seed_database(force=True)'",
    "build": "cd frontend && npm run build"
  }
}
